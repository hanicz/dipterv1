import { Serializable } from '../utils/serializable';

export class Finance{
    public id: Number;
    public finance_date: Date;
    public amount: number;
    public comment: String;
    public finance_type_id: Number;
}